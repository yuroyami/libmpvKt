# Module libmpvkt-canvas

`MpvCanvas`, mpv drawn as an ordinary Compose image through mpv's render API.

Experimental: nothing about its speed has been measured on hardware yet. `docs/compose-canvas.md` holds the empty measurement table and the numbers it has to reach.
